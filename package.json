{
  "name": "axiom-website",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "deploy": "node server.js && gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "firebase-tools": "^11.19.0"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}
